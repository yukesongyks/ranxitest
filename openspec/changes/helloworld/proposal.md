# Proposal: Hello World Endpoint

## Intent
Add a simple "Hello, World!" REST endpoint to the Spring Boot application.

## Scope
- Add a new `HelloWorldController` with a `GET /hello` endpoint that returns `"Hello, World!"`.
- Add a corresponding unit test.

## Out of Scope
- No authentication, no database, no complex logic.