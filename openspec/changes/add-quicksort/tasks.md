# Tasks: Add QuickSort

- [x] 1. Create `QuickSort.java` utility class in `com.example.myapp.utils` package
- [x] 2. Create `QuickSortTest.java` unit tests covering edge cases
- [x] 3. Verify compilation and run tests (static code review — Maven not available in environment)