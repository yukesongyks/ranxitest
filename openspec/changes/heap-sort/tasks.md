# Tasks: Heap Sort Implementation

- [x] 1. Create `HeapSort.java` in `com.example.myapp.sorting` package with generic heap sort implementation
- [x] 2. Create `HeapSortTest.java` with unit tests covering edge cases
- [x] 3. Run tests and verify all pass (static review — build env unavailable)