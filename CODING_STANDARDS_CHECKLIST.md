# 数科 Java 编码规范合规自检清单

> **生成说明**：本文件由 `/dtazziboot-java-coding-standards` 技能在缺乏有效需求输入时自动生成，作为编码规范基准参照。后续有效需求应严格遵循此清单及 SKILL.md 定义的五阶段工作流。

## 1. 强制执行规则
- [ ] **逐模块推进**：必须按顺序完成每个模块的五个阶段（READ/TEST/IMPL/CHECK/DOCS），输出进度追踪表。
- [ ] **代码可验证**：生成的代码必须语法正确，条件允许时执行编译和单测验证。
- [ ] **产出位置约定**：用户必须在调用时指定产出文件路径（OUTPUT_FILE），代码与文档同步更新。

## 2. 五阶段工作流检查点

### 阶段一：READ（读取上下文）
- [ ] 读取 `SSOT.md`（若存在）获取架构/模块文档路径及项目规范位置。
- [ ] 读取系分方案或需求描述。
- [ ] 按需加载 references 规范文档（命名、异常日志、网关、安全等）。
- [ ] 输出模块职责、关键类列表、依赖关系及已加载规范清单。

### 阶段二：TEST（生成单测）
- [ ] **TDD 强制**：在实现业务代码之前，必须先生成单元测试。
- [ ] 确定被测类（SUT）：通常为 Service 实现类。
- [ ] 读取 `references/unit-testing.md` 规范。
- [ ] 生成测试类 `src/test/java/.../{ClassName}Test.java`，遵循 AAA 模式。
- [ ] 覆盖正常路径、参数校验、数据不存在等核心场景。

### 阶段三：IMPL（实现代码）
- [ ] 按 Entity → Mapper → Service → Controller 顺序实现。
- [ ] 确保代码语法正确、依赖存在。
- [ ] 条件允许时运行 `mvn compile -DskipTests` 验证编译通过。
- [ ] 符合命名、异常、安全、网关等所有已加载规范。

### 阶段四：CHECK（规范检查）
- [ ] **L1 静态检查（必做）**：逐项检查命名、网关字段、异常日志、安全规范符合情况。
- [ ] **L2 动态验证（条件执行）**：探测环境并执行 `mvn compile` / `mvn test`；失败则标记跳过原因。
- [ ] 输出待人工验证命令清单。
- [ ] 发现问题立即修复，不遗留到下一阶段。

### 阶段五：DOCS（更新文档与产出报告）
- [ ] 根据 SSOT.md 或默认路径约定更新架构文档与模块文档。
- [ ] 新增模块时同步更新模块列表。
- [ ] 模块文档包含：模块职责、关键类说明、依赖关系、API 接口列表。
- [ ] 编码报告写入指定的 OUTPUT_FILE。

## 3. 核心规范索引（按需加载）
| 维度 | 参考文件 | 触发场景 |
|------|----------|----------|
| 命名 | references/naming.md | 新建类/方法时 |
| 异常日志 | references/exception-logging.md | 异常处理时 |
| 单元测试 | references/unit-testing.md | **每个模块必读** |
| 安全 | references/security.md | SQL/输入校验/敏感数据 |
| 数科网关 | references/antdigital-gateway.md | API 接口开发 |
| 集合处理 | references/collections.md | 集合操作时 |
| 并发处理 | references/concurrency.md | 多线程/虚拟线程 |
| JDK 21 | references/jdk21-coding-standards/ | 使用新特性时 |

## 4. 降级与异常处理协议
- 构建/测试超时或连续失败 ≥2 次 → 切换为静态代码审查，禁止重试或修改无关文件。
- 时间预算 <20% 或工具调用达上限 80% → 立即停止，输出降级报告。
- 需求缺失或产物路径未指定 → 生成本自检清单作为合规基线产物，禁止编造代码。

---
*Generated by DTCoder via dtazziboot-java-coding-standards skill | 2026-08-06*
